# Per-environment differences only. Names derive from prefix + lane + environment.

environment = "test"

# Test shares the single F2 by default. To isolate it, add a dedicated capacity
# to var.capacities in the platform root and set capacity_key to match.
